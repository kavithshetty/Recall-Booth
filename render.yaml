services:
  - type: web
    runtime: static
    name: recall-booth
    buildCommand: "true"
    staticPublishPath: .
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
